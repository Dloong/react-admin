import { PluginPrettierOptions } from '@walrus/types';
/**
 * 美化程序封装
 * @param currentDirectory 当前目录
 * @param options 配置参数
 */
declare const prettier: (currentDirectory: string, options?: PluginPrettierOptions) => {
    success: boolean;
    errors: unknown[];
};
export default prettier;
