"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = checkNodeVersion;

var _chalk = _interopRequireDefault(require("chalk"));

var _semver = require("semver");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * 检查Node版本
 * @param wanted 需要兼容的版本
 * @param id 需要提示的 cli name
 */
function checkNodeVersion(wanted, id) {
  if (!(0, _semver.satisfies)(process.version, wanted)) {
    console.log(_chalk.default.red('You are using Node ' + process.version + ', but this version of ' + id + ' requires Node ' + wanted + '.\nPlease upgrade your Node version.'));
    process.exit(1);
  }
}