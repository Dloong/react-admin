/**
 * 转换url为数组
 *
 * @param url string
 * @returns {string[]}
 * @example
 * ```
 * urlToList(test)
 * // => ['/test']
 *
 * urlToList(/admin/user)
 * // => ['/test', '/test/user']
 * ```
 */
export default function urlToList(url: string): string[];
