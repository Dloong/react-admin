import React from 'react';
import { ButtonProps } from 'antd/es/button';
export interface ISendCodeProps extends ButtonProps {
    start?: boolean;
    second?: number;
    initText?: string;
    runText?: string;
    resetText?: string;
    onEnd?: () => void;
}
interface ISendCodeState {
    loading: boolean;
    status: number;
    second: number | undefined;
}
export interface ISendCodeLocale {
    initText: string;
    runText: string;
    resetText: string;
}
declare class SendCode extends React.Component<ISendCodeProps, ISendCodeState> {
    private timer;
    constructor(props: any);
    static defaultProps: ISendCodeProps;
    readonly state: ISendCodeState;
    componentWillUnmount(): void;
    componentWillReceiveProps(nextProps: ISendCodeProps): void;
    startCountdown: () => void;
    timeout: () => void;
    buttonText: (sendCodeLocale: ISendCodeLocale) => string;
    renderSendCode: (sendCodeLocale: ISendCodeLocale) => JSX.Element;
    render(): JSX.Element;
}
export default SendCode;
