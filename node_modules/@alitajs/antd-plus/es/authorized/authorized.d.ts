import React from 'react';
import { Authority, Policy } from './check-authority';
export interface AuthorizedProps {
    authority?: Authority;
    noMatch?: React.ReactNode;
    policy?: Policy;
}
declare const Authorized: React.FC<AuthorizedProps>;
export default Authorized;
