import React from 'react';
import { TooltipProps, AxisProps, LegendProps } from 'bizcharts';
import { TPadding } from '@/global';
import { TPosition } from '../components/title';
import './area-chart.less';
export interface IDataItem {
    x: number | string;
    [key: string]: number | string;
}
export interface IAreaProps {
    className?: string;
    title?: string;
    titlePosition?: TPosition;
    style?: React.CSSProperties;
    colors?: string[];
    height?: number;
    padding?: TPadding;
    line?: boolean;
    point?: boolean;
    animate?: boolean;
    xAxis?: AxisProps;
    forceFit?: boolean;
    scale?: any;
    isStack?: boolean;
    tooltip?: TooltipProps;
    legend?: LegendProps;
    yAxis?: Partial<AxisProps>;
    borderWidth?: number;
    data: IDataItem[];
    titleMap?: {
        [key: string]: any;
    };
    mini?: boolean;
    smooth?: boolean;
}
declare const AreaChart: React.FC<IAreaProps>;
export default AreaChart;
