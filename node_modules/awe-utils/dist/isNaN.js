'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 检查`value`是否为`NaN`。
 *
 * @since 0.0.6
 *
 * @param value
 * @returns {boolean}
 *
 * @example
 *
 * isNaN(NaN)
 * // => true
 *
 * isNull(1)
 * // => false
 */
function isNaN(value) {
  return value !== value;
}

exports.isNaN = isNaN;
