/**
 * 转换url为数组
 *
 * @since 0.0.5
 *
 * @param url string
 * @returns {string[]}
 *
 * @example
 *
 * urlToList(test)
 * // => ['/test']
 *
 * urlToList(/admin/user)
 * // => ['/test', '/test/user']
 */
export declare function urlToList(url: string): string[];
